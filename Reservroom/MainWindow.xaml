<Window x:Class="Reservroom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reservroom" 
        xmlns:views="clr-namespace:Reservroom.Views" 
        xmlns:viewmodels="clr-namespace:Reservroom.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="Reservroom"
        Height="450" 
        Width="800"
        FontSize="14">
    <Grid Margin="20 10" MaxWidth="600">
        <views:ReservationListringView DataContext="{Binding CurrentViewModel}"/>
        <!--<views:ReservationListringView/>-->
    </Grid>
</Window>
